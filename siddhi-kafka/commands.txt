Step 1: Start Kafka + Siddhi containers
cd path\to\your\project
docker-compose up -d


-d runs in detached mode.

Check logs to verify startup:

docker-compose logs -f kafka
docker-compose logs -f siddhi

Step 2: Run the ML consumer

Open a new terminal (host machine):

python consumer.py


You should see:

✅ Consumer started. Waiting for messages...

Step 3: Run the Kafka producer

Open another terminal (host machine):

python producer.py


You should see each row being sent:

📤 Sent: {'temperature_2m (°C)': 22.1, 'relative_humidity_2m (%)': 55.3, ... }


Step 4: Monitor Siddhi logs
docker-compose logs -f siddhi


You should see alerts being logged:

ALERT {"sensorId":"ml-forecast","temperature":22.1}

Step 5: Optional — test Siddhi HTTP manually
curl -X POST http://localhost:8006/SensorStream -H "Content-Type: application/json" -d '{"sensorId":"test","temperature":20.5}'


Should also appear in Siddhi logs. ✅

Step 6: Stop everything (after testing)
docker-compose down
